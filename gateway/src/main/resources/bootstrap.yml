server:
  port: 7777
spring:
  application:
    # 注册时的服务ID
    name: gateway
zuul:
  #全部服务忽略敏感头(全部服务都可以传递cookie)
  sensitive-headers:
  routes:
    mutidatasource:
      path: /muti/**
      serviceId: mutidatasource
      sensitiveHeaders:
  #简洁写法
    singledatasource: /single/**